# Optional Grafana SMTP settings. Leave disabled for local/VPS smoke use.
GF_SMTP_ENABLED=false
GF_SMTP_HOST=
GF_SMTP_USER=
GF_SMTP_PASSWORD=
GF_SMTP_FROM_ADDRESS=

# Public Grafana URL used in links and alert notifications.
GF_SERVER_DOMAIN=localhost
GF_SERVER_ROOT_URL=http://localhost:3000
